import test from 'node:test';import assert from 'node:assert/strict';
import {safeCategory,sanitizeEvent} from '../supabase/functions/_shared/telemetry.ts';
test('one category rule refuses a name, an email, and a URL',()=>{for(const value of ['Jane Smith','jane@example.com','https://example.com/profile']){assert.equal(safeCategory(value,[value]),false);assert.throws(()=>sanitizeEvent({event_name:'capture_saved',reason:value}));}});
test('telemetry accepts only the closed vocabulary and bounded primitives',()=>{assert.deepEqual(sanitizeEvent({event_name:'capture_saved',approach:'manual',source:'web',reason:'user_action',count:1,success:true}),{event_name:'capture_saved',approach:'manual',source:'web',reason:'user_action',count:1,success:true});for(const input of [{event_name:'capture_saved',note:'secret'},{event_name:'capture_saved',count:NaN},{event_name:'capture_saved',count:-1},{event_name:'unknown'},{event_name:'capture_saved',source:'linkedin.com'},['capture_saved'],null])assert.throws(()=>sanitizeEvent(input));});
